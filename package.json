{
  "name": "silkmoon-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "ar-service"
  ],
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND,AR-SERVICE\" -c \"cyan.bold,yellow.bold,magenta.bold\" \"npm run start:dev -w backend\" \"npm run dev -w frontend\" \"npm run dev -w ar-service\"",
    "build": "npm run build --workspaces --if-present",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
